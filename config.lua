Config = {}

-- Touches par défaut à désactiver (36 = CTRL)
Config.DisabledControl = 36

-- Couleurs des messages
Config.Colors = {
    success = {0, 255, 0},   -- Vert
    error = {255, 0, 0}      -- Rouge
}

-- Messages personnalisables
Config.Messages = {
    enabled = "Mode combat désactivé",
    disabled = "Mode combat activé"
}